name: Keep Render App Awake

on:
  schedule:
    - cron: "*/15 * * * *"   # runs every hour
  workflow_dispatch:      # allows manual run

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render app
        run: |
          curl -L https://pycompiler-zbjs.onrender.com
